{
  "name": "instagram-dm-funnel",
  "version": "1.0.0",
  "description": "Free ManyChat-style Instagram comment-to-DM funnel with a follow gate — runs on Cloudflare Workers' free tier",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
